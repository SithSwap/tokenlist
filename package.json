{
  "name": "@sithswap/tokenlist",
  "version": "1.0.0",
  "description": "The default SithSwap token list 🫳",
  "author": "0xSidius",
  "license": "GPL-3.0-or-later",
  "keywords": [
    "sithswap",
    "default",
    "token",
    "list"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sithswap/tokenlist.git"
  },
  "files": [
    "build/sithswap.tokenlist.json"
  ],
  "main": "build/sithswap.tokenlist.json",
  "scripts": {
    "test": "ts-mocha test/*.test.ts",
    "build": "rimraf build && mkdir -p build && ts-node src/write.ts > build/sithswap.tokenlist.json",
    "prepublishOnly": "npm test && npm run build"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "rimraf": "^4.4.1",
    "starknet": "^4.22.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.1",
    "tslib": "^2.5.0",
    "typescript": "^5.0.3"
  }
}
